{
  "name": "@ethernick/statamic-activitypub-questions",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build:v5": "cross-env BUILD_ADDON=true VUE_VERSION=2 vite build",
    "build": "npm run build:v5",
    "production": "npm run build"
  },
  "devDependencies": {
    "@vitejs/plugin-vue2": "^2.3.4",
    "cross-env": "^7.0.3",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "vue": "^2.7.16"
  }
}
